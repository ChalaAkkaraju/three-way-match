# Optional. Without it the app runs on the synthetic corpus and the reviewer
# UI reports that PDF uploads are disabled. With it, dropping a PDF on the
# queue sends the document to Claude for field extraction.
ANTHROPIC_API_KEY=

# Railway sets PORT itself. Only needed for local runs on a different port.
# PORT=8000
